"use client";
import { ReactNode, useState } from "react";
import SiteFooter from "./SiteFooter";
import ProgressiveImage from "./ProgressiveImage";
export default function SiteFrame({active: _active,children}:{active:string;children:ReactNode}){const[contactOpen,setContactOpen]=useState(false),[copied,setCopied]=useState(false);const copyWechat=async()=>{await navigator.clipboard.writeText("RickLiu1992");setCopied(true);setTimeout(()=>setCopied(false),1800)};return <main className="inner-site">{children}<SiteFooter/><button className="floating-contact" onClick={()=>setContactOpen(true)} aria-label="咨询合作"><span aria-hidden="true">🤝</span>咨询合作</button>{contactOpen&&<div className="modal-backdrop" onMouseDown={()=>setContactOpen(false)}><div className="contact-modal" onMouseDown={e=>e.stopPropagation()}><button className="modal-close" onClick={()=>setContactOpen(false)}>×</button><p className="eyebrow dark"><span/> BUSINESS CONTACT</p><h2>微信咨询</h2><ProgressiveImage src="/assets/contact/wechat-business.png" optimizedBase="/assets/optimized/contact/wechat-business" width={760} height={760} sizes="230px" alt="商务合作微信二维码"/><strong>请注明公司名称以及来意，谢谢</strong><div className="copy-row"><code>RickLiu1992</code><button onClick={copyWechat}>{copied?"微信号已复制":"复制微信号"}</button></div><a className="modal-email" href="mailto:rick@sellswell.cn">rick@sellswell.cn ↗</a></div></div>}</main>}
export function PageHero({eyebrow,title,description}:{eyebrow:string;title:ReactNode;description:string}){return <section className="inner-hero"><div className="inner-grid"/><div className="inner-hero-copy"><p className="eyebrow"><span/>{eyebrow}</p><h1>{title}</h1><p>{description}</p></div><div className="inner-index">SELLSWELL<br/>GLOBAL E-COMMERCE</div></section>}
